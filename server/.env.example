PORT=5000

DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database
DB_USER=your_user
DB_PASSWORD=your_password

# Access token — keep short (15m, 30m)
JWT_SECRET=your_access_token_secret
ACCESS_TOKEN_EXPIRES_IN=15m

# Refresh token — how many days until it expires
REFRESH_TOKEN_DAYS=7
